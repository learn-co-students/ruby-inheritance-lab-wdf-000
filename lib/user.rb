class User

  attr_accessor :first_name, :last_name, :new

  def intialize(first_name, last_name)
    @first_name = name
    @last_name = name
    @new = new_student
  end

  def new_student
    @new = "#{first_name} #{last_name}"
  end
end
